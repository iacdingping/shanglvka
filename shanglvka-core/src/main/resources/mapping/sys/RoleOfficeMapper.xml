<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.slk.core.dao.sys.RoleOfficeDao">
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="sys-RoleOffice-columns">
	    <![CDATA[
		`role_id` as `roleId`, `office_id` as `officeId`
	    ]]>
	</sql>	

	<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
	<insert id="save" useGeneratedKeys="true" keyProperty="roleId">
    <![CDATA[
        INSERT INTO sys_role_office (
        `role_id`, `office_id`
        ) VALUES (
        #{roleId}, #{officeId}        
        )
    ]]>
		<!--	
			oracle: order="BEFORE" SELECT sequenceName.nextval AS ID FROM DUAL 
			DB2: order="BEFORE"" values nextval for sequenceName
		<selectKey resultType="java.lang.Long" order="BEFORE" keyProperty="userId">
			SELECT sequenceName.nextval AS ID FROM DUAL 
        </selectKey>
		-->
	</insert>
    
	<update id="update">
    <![CDATA[
        UPDATE sys_role_office SET
	        
        WHERE 
        	role_id = #{roleId}  AND  office_id = #{officeId}  	        
    ]]>
	</update>

    <delete id="deleteById">
    <![CDATA[
        DELETE FROM sys_role_office WHERE
        `role_id` = #{id}  AND 
        `office_id` = #{id} 
    ]]>
    </delete>
    
    <select id="getById" resultType="RoleOffice">
		SELECT <include refid="sys-RoleOffice-columns" />
	    <![CDATA[
		    FROM sys_role_office 
	        WHERE 
		        `role_id` = #{id}  AND 
		        `office_id` = #{id} 
	    ]]>
	</select>
	
	<sql id="RoleOffice-where">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>	      				
	       <if test="roleId">
				AND `role_id` = #{roleId}
			</if>
	       <if test="officeId">
				AND `office_id` = #{officeId}
			</if>
		</where>
	</sql>
		
    <select id="count" resultType="long">
        SELECT count(*) FROM sys_role_office 
		<include refid="RoleOffice-where"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findPage" resultType="RoleOffice">
		SELECT <include refid="sys-RoleOffice-columns" /> FROM (
			SELECT <include refid="sys-RoleOffice-columns" />
			FROM sys_role_office 
			<include refid="RoleOffice-where"/>
			
			<if test="sortColumns != null">
				ORDER BY ${sortColumns}
			</if>
		) limit #{offset}, #{pageSize}
    </select>
	
	<select id="list" resultType="RoleOffice">
    	SELECT <include refid="sys-RoleOffice-columns" />
	    FROM sys_role_office 
		<include refid="RoleOffice-where"/>
		
		<if test="sortColumns != null">
			ORDER BY ${sortColumns}
		</if>
    </select>

	
</mapper>

