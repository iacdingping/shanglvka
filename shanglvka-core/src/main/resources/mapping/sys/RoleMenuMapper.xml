<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.slk.core.dao.sys.RoleMenuDao">
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="sys-RoleMenu-columns">
	    <![CDATA[
		`role_id` as `roleId`, `menu_id` as `menuId`
	    ]]>
	</sql>	

	<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
	<insert id="save" useGeneratedKeys="true" keyProperty="roleId">
    <![CDATA[
        INSERT INTO sys_role_menu (
        `role_id`, `menu_id`
        ) VALUES (
        #{roleId}, #{menuId}        
        )
    ]]>
		<!--	
			oracle: order="BEFORE" SELECT sequenceName.nextval AS ID FROM DUAL 
			DB2: order="BEFORE"" values nextval for sequenceName
		<selectKey resultType="java.lang.Long" order="BEFORE" keyProperty="userId">
			SELECT sequenceName.nextval AS ID FROM DUAL 
        </selectKey>
		-->
	</insert>
    
	<update id="update">
    <![CDATA[
        UPDATE sys_role_menu SET
	        
        WHERE 
        	role_id = #{roleId}  AND  menu_id = #{menuId}  	        
    ]]>
	</update>

    <delete id="deleteById">
    <![CDATA[
        DELETE FROM sys_role_menu WHERE
        `role_id` = #{id}  AND 
        `menu_id` = #{id} 
    ]]>
    </delete>
    
    <select id="getById" resultType="RoleMenu">
		SELECT <include refid="sys-RoleMenu-columns" />
	    <![CDATA[
		    FROM sys_role_menu 
	        WHERE 
		        `role_id` = #{id}  AND 
		        `menu_id` = #{id} 
	    ]]>
	</select>
	
	<sql id="RoleMenu-where">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>	      				
	       <if test="roleId">
				AND `role_id` = #{roleId}
			</if>
	       <if test="menuId">
				AND `menu_id` = #{menuId}
			</if>
		</where>
	</sql>
		
    <select id="count" resultType="long">
        SELECT count(*) FROM sys_role_menu 
		<include refid="RoleMenu-where"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findPage" resultType="RoleMenu">
		SELECT <include refid="sys-RoleMenu-columns" /> FROM (
			SELECT <include refid="sys-RoleMenu-columns" />
			FROM sys_role_menu 
			<include refid="RoleMenu-where"/>
			
			<if test="sortColumns != null">
				ORDER BY ${sortColumns}
			</if>
		) limit #{offset}, #{pageSize}
    </select>
	
	<select id="list" resultType="RoleMenu">
    	SELECT <include refid="sys-RoleMenu-columns" />
	    FROM sys_role_menu 
		<include refid="RoleMenu-where"/>
		
		<if test="sortColumns != null">
			ORDER BY ${sortColumns}
		</if>
    </select>

	
</mapper>

