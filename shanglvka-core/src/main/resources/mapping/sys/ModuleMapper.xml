<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.slk.core.dao.sys.ModuleDao">
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="sys-Module-columns">
	    <![CDATA[
		`id` as `id`, `parent` as `parent`, `name` as `name`, `icon` as `icon`, `expanded` as `expanded`, `type` as `type`, `permission` as `permission`, `order` as `order`, `created` as `created`, `deleted` as `deleted`, `last_operate_time` as `lastOperateTime`
	    ]]>
	</sql>	

	<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
	<insert id="save" useGeneratedKeys="true" keyProperty="id">
    <![CDATA[
        INSERT INTO sys_module (
        `id`, `parent`, `name`, `icon`, `expanded`, `type`, `permission`, `order`, `created`, `deleted`, `last_operate_time`
        ) VALUES (
        #{id}, #{parent}, #{name}, #{icon}, #{expanded}, #{type}, #{permission}, #{order}, #{created}, #{deleted}, #{lastOperateTime}        
        )
    ]]>
		<!--	
			oracle: order="BEFORE" SELECT sequenceName.nextval AS ID FROM DUAL 
			DB2: order="BEFORE"" values nextval for sequenceName
		<selectKey resultType="java.lang.Long" order="BEFORE" keyProperty="userId">
			SELECT sequenceName.nextval AS ID FROM DUAL 
        </selectKey>
		-->
	</insert>
    
	<update id="update">
    <![CDATA[
        UPDATE sys_module SET
	        `parent` = #{parent} , `name` = #{name} , `icon` = #{icon} , `expanded` = #{expanded} , `type` = #{type} , `permission` = #{permission} , `order` = #{order} , `created` = #{created} , `deleted` = #{deleted} , `last_operate_time` = #{lastOperateTime}  
        WHERE 
        	id = #{id}  	        
    ]]>
	</update>

    <delete id="deleteById">
    <![CDATA[
        DELETE FROM sys_module WHERE
        `id` = #{id} 
    ]]>
    </delete>
    
    <select id="getById" resultType="Module">
		SELECT <include refid="sys-Module-columns" />
	    <![CDATA[
		    FROM sys_module 
	        WHERE 
		        `id` = #{id} 
	    ]]>
	</select>
	
	<sql id="Module-where">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>	      				
	       <if test="id">
				AND `id` = #{id}
			</if>
	       <if test="parent">
				AND `parent` = #{parent}
			</if>
	       <if test="name">
				AND `name` = #{name}
			</if>
	       <if test="icon">
				AND `icon` = #{icon}
			</if>
	       <if test="expanded">
				AND `expanded` = #{expanded}
			</if>
	       <if test="type">
				AND `type` = #{type}
			</if>
	       <if test="permission">
				AND `permission` = #{permission}
			</if>
	       <if test="order">
				AND `order` = #{order}
			</if>
	       <if test="createdBegin != null">
				AND `created` >= #{createdBegin}
		   </if>
		   <if test="createdEnd != null">
				AND `created` &lt;= #{createdEnd}
		   </if>
	       <if test="deleted">
				AND `deleted` = #{deleted}
			</if>
	       <if test="lastOperateTimeBegin != null">
				AND `last_operate_time` >= #{lastOperateTimeBegin}
		   </if>
		   <if test="lastOperateTimeEnd != null">
				AND `last_operate_time` &lt;= #{lastOperateTimeEnd}
		   </if>
		</where>
	</sql>
		
    <select id="count" resultType="long">
        SELECT count(*) FROM sys_module 
		<include refid="Module-where"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findPage" resultType="Module">
		SELECT <include refid="sys-Module-columns" /> FROM (
			SELECT <include refid="sys-Module-columns" />
			FROM sys_module 
			<include refid="Module-where"/>
			
			<if test="sortColumns != null">
				ORDER BY ${sortColumns}
			</if>
		) limit #{offset}, #{pageSize}
    </select>
	
	<select id="list" resultType="Module">
    	SELECT <include refid="sys-Module-columns" />
	    FROM sys_module 
		<include refid="Module-where"/>
		
		<if test="sortColumns != null">
			ORDER BY ${sortColumns}
		</if>
    </select>

	
</mapper>

